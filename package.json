{
  "private": true,
  "name": "storm-the-front",
  "workspaces": ["packages/*"],
  "scripts": {
    "fe:start": "yarn workspace client start",
    "fe:build:prod": "yarn workspace client build:prod",
    "fe:build:dev": "yarn workspace client build:dev",
    "fe:lint": "yarn workspace client eslint",
    "fe:test": "yarn workspace client jest",
    "be:start": "yarn workspace server start"
  }
}