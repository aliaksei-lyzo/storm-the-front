import React from 'react';
import Menu from 'components/Menu';

const Header = () => (
  <div>
    HEADER
    <Menu />
  </div>
);

/* propTypes declaration */
Header.propTypes = {};

export default Header;
